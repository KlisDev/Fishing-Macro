__pycache__/
*.pyc
.venv/
venv/
debug/
*.mkv
*.mp4
.deps_installed
config.json
diag/
.claude/

# Diagnostic captures. Recordings for tools/vision_suite.py live OUTSIDE the
# project folder entirely — it gets zipped and sent to testers, so a few
# hundred MB of gameplay (showing usernames, levels and balances) must not be
# sitting in it.
clips/
record/

# --dev capture folders and the per-machine fish template
capture_*/
fish_template.png

# F8 diagnostic session logs
diag_session_*.log
